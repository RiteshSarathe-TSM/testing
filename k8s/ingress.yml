apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: service1-ingress
spec:
  rules:
  - host: matorg.com
    http:
      paths:
      - path: /ping
        pathType: Prefix
        backend:
          service:
            name: service1
            port:
              number: 80
